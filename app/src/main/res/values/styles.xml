<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="searchViewStyle">@style/CustomSearchStyle</item>
    </style>

    <style name="CustomSearchStyle" parent="Base.Widget.AppCompat.SearchView.ActionBar">
        <item name="suggestionRowLayout">@layout/abc_search_dropdown_item_icons</item>
    </style>

    <style name="CustomSearchView" parent="@android:style/Widget.Holo">
        <item name="android:textColorHint">#CCCCCC</item>
    </style>

    <style name="SearchResult.Title" parent="@android:style/TextAppearance.Large.Inverse">
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:textSize">30sp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

</resources>